This is the GOES GVAR Stream reading library.  It is used to parse the
digital stream from GOES satelites.  This is the first public version,
and is designed for GOES satelittes versions .

More information regarding the GVAR stream can be found:

./configure --prefix=<the directory you want the library to be generated>
make
make install

Developers can recreate the autogenerated files by running 

./bootstrap

